{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "build-extension",
			"type": "shell",
			"command": "bun run build",
			"options": {
				"cwd": "${workspaceFolder}/packages/vscode-extension"
			},
			"group": "build",
			"presentation": {
				"reveal": "silent"
			}
		}
	]
}
